$('#editCategoryModal').modal('hide');
$('body').removeClass('modal-open');
$('.modal-backdrop').remove();
$('#categoryShow').html("<%= escape_javascript(render partial: 'catalog_categories/category_index', locals: {:@categories => @categories}) %>");